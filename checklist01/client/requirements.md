## Packages
date-fns | Date manipulation for the daily grid columns and navigation

## Notes
- The app requires a date-based grid layout.
- Entries are stored as sparse data (only set values), defaulting to "ー" visually if missing.
- "Display Days" logic will need to dynamically generate the date range columns.
- Tailwind config might need extension for specific status colors if standard palette isn't enough.
